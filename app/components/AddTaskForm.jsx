import { View, Text, ScrollView } from 'react-native'
import React from 'react'

const AddTaskForm = () => {
    //Task name, 
  return (
    <ScrollView>
    </ScrollView>
  )
}

export default AddTaskForm
